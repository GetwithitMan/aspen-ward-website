{
  "name": "aspen-ward-website",
  "version": "2.0.0",
  "description": "Sunday worship services website for Aspen Ward",
  "type": "module",
  "scripts": {
    "dev": "npx http-server -p 8080 -c-1",
    "test": "node --test js/utils/*.test.js",
    "lint": "npx eslint js/**/*.js",
    "build": "npm run build:css && npm run build:js",
    "build:css": "npx lightningcss --minify --bundle --targets '>= 0.25%' css/variables.css css/reset.css css/layout.css css/components.css css/animations.css -o dist/styles.min.css",
    "build:js": "npx esbuild js/app.js --bundle --minify --target=es2020 --outfile=dist/app.min.js",
    "build:prod": "npm run build && npm run optimize",
    "optimize": "echo 'Run image optimization and compression here'",
    "serve": "npx http-server dist -p 8080"
  },
  "keywords": [
    "church",
    "ward",
    "lds",
    "worship"
  ],
  "author": "Aspen Ward Members",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.0",
    "eslint": "^8.50.0",
    "http-server": "^14.1.1",
    "lightningcss": "^1.22.0"
  }
}
